<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="tnde.list">
      <field name="name">tnde list</field>
      <field name="model">tnde.tnde</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="tnde.action_window">
      <field name="name">tnde window</field>
      <field name="res_model">tnde.tnde</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="tnde.action_server">
      <field name="name">tnde server</field>
      <field name="model_id" ref="model_tnde_tnde"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="tnde" id="tnde.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="tnde.menu_1" parent="tnde.menu_root"/>
    <menuitem name="Menu 2" id="tnde.menu_2" parent="tnde.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="tnde.menu_1_list" parent="tnde.menu_1"
              action="tnde.action_window"/>
    <menuitem name="Server to list" id="tnde" parent="tnde.menu_2"
              action="tnde.action_server"/>
    -->
  </data>
</odoo>